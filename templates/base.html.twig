<!DOCTYPE html>
<html lang="{{ app.request.locale }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
    <title>{% block title %}Job board for Symfony{% endblock %}</title>
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</head>
<body style="background-color: #f3f4f6">
{% block body %}
    <div class="container">
        <header class="py-3">
            <div class="row flex-nowrap justify-content-between align-items-center">
                <div class="col-4">
                    <a class="text-dark text-decoration-none navbar-brand" style="font-size: 2rem" href="{{ path('job_index') }}">Job board for Symfony</a>
                </div>
                <div class="col-4 d-flex justify-content-end align-items-center">
                    <a class="btn btn-outline-primary" href="{{ path('job_new') }}">{{ 'action.post_job'|trans }}</a>
                </div>
            </div>
        </header>
    </div>
    <main class="container">
        {% block main %}{% endblock %}
    </main>
    <footer class="text-muted py-5">
        <div class="container">
            <p class="float-end mb-1">
                <a href="#">Back to top</a>
            </p>
            <small class="mb-1 d-block">Unofficial job board for Symfony.</small>
            <small class="mb-0">Symfony is a trademark please refer to <a href="https://symfony.com/trademark">Trademark and Logo Policy</a>.</small>
        </div>
    </footer>
{% endblock %}
</body>
</html>
