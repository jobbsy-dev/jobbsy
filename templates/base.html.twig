<!DOCTYPE html>
<html lang="{{ app.request.locale }}">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1"/>
        <title>{% block title %}Jobbsy{% endblock %}</title>
        <link rel="icon" href="{{ asset('build/images/icon.svg') }}">
        <meta name="description" content="Looking for a Symfony developer job? or hire Symfony developer ? jobbsy.dev is the place to be.">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}

        {% block extra_scripts %}
            {% if app.environment is same as 'prod' %}
                <script defer data-domain="jobbsy.dev" src="https://analytics.jobbsy.dev/js/plausible.js"></script>
            {% endif %}
        {% endblock %}
    </head>
    <body>
        {% block body %}
            <header class="px-2 py-4">
                <div class="container">
                    <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
                        <a href="{{ path('job_index') }}" class="mb-2 mb-sm-0 mb-md-0">
                            <img src="{{ asset('build/images/logo_jobbsy.svg') }}" alt="logo" width="120">
                        </a>
                        <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0 ms-md-3">
                            <li class="nav-item">
                                <a href="{{ path('job_index') }}" class="nav-link px-2 {% if app.current_route != 'job_index'%}link-secondary{% endif %}">
                                    Jobs
                                </a>
                            </li>
                            <li class="nav-item">
                                <a href="{{ path('event_index') }}" class="nav-link px-2 {% if app.current_route != 'event_index'%}link-secondary{% endif %}">
                                    Events
                                </a>
                            </li>
                            <li class="nav-item">
                                <a href="{{ path('news_index') }}" class="nav-link px-2 {% if app.current_route != 'news_index'%}link-secondary{% endif %}">
                                    News
                                </a>
                            </li>
                            {#                            <li class="nav-item">#}
                            {#                                <a href="{{ path('blog_index') }}" class="nav-link px-2 {% if routeName != 'app_blog_index'%}link-secondary{% endif %}">#}
                            {#                                    Blog#}
                            {#                                </a>#}
                            {#                            </li>#}
                        </ul>
                        {% block post_job %}
                            <div class="ms-3 ms-md-0 d-none d-lg-block">
                                <a href="{{ path('job_new') }}" class="btn btn-primary px-3 bg-gradient">{{ 'action.post_job'|trans }}</a>
                            </div>
                        {% endblock %}
                    </div>
                </div>
            </header>

            <main class="container pb-2 pb-md-5">
                {{ include('default/_flash_messages.html.twig') }}

                {% block main %}{% endblock %}
            </main>

            <footer class="py-3 px-3 px-md-0 py-md-5 text-center border-top bg-light">
                <ul class="nav justify-content-center pb-3 mb-1 ">
                    <li class="nav-item">
                        <a href="{{ path('job_index') }}" class="nav-link px-2 text-muted">Home</a>
                    </li>
                    <li class="nav-item">
                        <a href="mailto:hello@jobbsy.dev" class="nav-link px-2 text-muted">Contact</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('job_rss') }}" class="nav-link px-2 text-muted">RSS</a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('api_doc') }}" class="nav-link px-2 text-muted">API</a>
                    </li>
                    <li class="nav-item">
                        <a href="https://github.com/jobbsy-dev/jobbsy" target="_blank" rel="nofollow" class="nav-link px-2 text-muted">Github</a>
                    </li>
                    <li class="nav-item">
                        <a href="https://github.com/orgs/jobbsy-dev/sponsoring" target="_blank" rel="nofollow" class="nav-link px-2 text-muted">Sponsoring</a>
                    </li>
                    <li class="nav-item">
                        <a href="https://twitter.com/jobbsy" target="_blank" rel="nofollow" class="nav-link px-2 text-muted">Twitter</a>
                    </li>
                </ul>
                <p class="text-center text-muted mb-0">{{ ''|date('Y') }} Jobbsy, open source job board for Symfony™</p>
                <small class="text-muted">Symfony™ is a trademark, please refer to <a href="https://symfony.com/trademark" class="text-reset" target="_blank" rel="nofollow">Trademark and Logo Policy</a>.</small>
            </footer>
        {% endblock %}
    </body>
</html>
